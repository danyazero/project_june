package main

@Getter(value="hi", name="e")
pub class Main : Function {
    const d [][]int = 12 * 2
    @Setter(value="hi!")
    @Getter
    const e string = "E constant"
    const m bool = false

    func main() {
        a := 2 * 3
        if a > 5 {
            a -= 5 * 2;
        }
        b := "Hi!"
        c := !false
        d := 2.53
    }

    func parse(a int, b int) []int {
        return (a + b) << 1
    }
}