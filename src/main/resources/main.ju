class Main {
    fn main(args []string) {
        array := [12, 5, 3, 24]
        result := 0

        for (index, item) in array {
            result += index * item
        }
        print!(result)
    }
}